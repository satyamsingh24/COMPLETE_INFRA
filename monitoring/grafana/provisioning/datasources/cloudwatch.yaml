apiVersion: 1

datasources:
  - name: CloudWatch
    type: cloudwatch
    access: proxy
    isDefault: true
    jsonData:
      authType: ec2_iam_role
      defaultRegion: ap-south-1
    version: 1
    editable: true
